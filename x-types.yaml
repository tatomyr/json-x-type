XType:
  - undefined
  - any
  - number # literal numbers
  - boolean # literal booleans
  - string # including `string` and string literals
  - null # literal `null` value
  - $ref: '#/Ref'
  - $ref: '#/Or'
  - $ref: '#/And'
  - $ref: '#/Array'
  - $ref: '#/Object'
Ref:
  $literal:$ref: string::uri
Or:
  $array:
    $ref: '#/XType'
And:
  $literal:$and:
    $ref: '#/Or'
Array:
  $literal:$array:
    $ref: '#/XType'
Object:
  $record: # including `$record` and any string literals, but excluding defined above: `$ref`, `$and`, and `$array`.
    $ref: '#/XType'
